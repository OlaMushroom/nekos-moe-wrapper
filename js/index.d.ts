/**
 * @module
 * @example
 * ```ts
 * import * as nekos from '@om/nekos-moe';
 * console.log(nekos._links);
 * ```
 */
export * from './main.ts';
/** URLs. */
export declare const _links: {
    website: {
        url: string;
        repo: string;
    };
    api: {
        url: string;
        repo: string;
        docs: {
            url: string;
            repo: string;
        };
    };
    wrapper: {
        url: string;
        repo: string;
        docs: {
            url: string;
            repo: string;
        };
    };
};
